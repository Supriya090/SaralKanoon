<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/experiences.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">

</head>
<body>
  <%if(isLoggedIn){%>
    <%- include('partials/loggedInHeader') %>
  <%}else{%>  
    <%- include('partials/header') %>
  <%}%>
    <div class="container expDiv"> 
        
        <!-- Search Button Section -->
        <div>
            <form class="form-inline my-2 my-lg-0 pull-right" method="GET" action="/searchExp" id="expForm">
              <input class="form-control mr-sm-2" type="search" placeholder="Search Tags" aria-label="Search" name="value" id="expSearchBar">
              <button class="btn btn-secondary my-2 my-sm-0" type="submit"  id="searchIcon"><i class="fa fa-search"></i></button>
            </form>
          </div>
        </div>
        <div class="container">
    <a href="/addExperience" class="shareLink" style="text-decoration: none;">
        <h5 class="expSharing" >Want to share your Story?</h5>
    </a>
</div>
        

    <% experienceList.forEach(function(experience, i) { %>
        <div class="card" style="width: 80%; margin: 10px auto; border-color: gray; background:url('https://images.unsplash.com/photo-1535955565956-4a631184a821?ixlib=rb-1.2.1&auto=format&fit=crop&w=967&q=80')
        no-repeat fixed;
        background-size: cover;">
            <div class="card-body">
              <h5 class="card-title">Experience #<%=i+1%></h5>
              <p class="card-text"><%=experience.content%></p>
              <h6 class="card-subtitle mb-2 text-muted">Tags:
                <%for(let i =0; i<(experience.tag.length); i++){%>
                  <span class="badge badge-secondary"><%=experience.tag[i]%></span>
                <%}%>
              </h6>
              <h6>By:<%=experience.user%></h6>
            </div>
          </div>
    <% }) %>
    </div>
    <%- include('partials/footer') %>
</body>
</html>